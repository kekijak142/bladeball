local TweenService = game:GetService("TweenService")
local UserInputService = game:GetService("UserInputService")
local CoreGui = game:GetService("CoreGui")
local RunService = game:GetService("RunService")
local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local VirtualInputManager = game:GetService("VirtualInputManager")
local LocalPlayer = Players.LocalPlayer

-- ==========================================
-- ASCEND HUB | CREATED BY KIMI 
-- ==========================================

-- [ Anti-Cheat Bypass Layer 1: Call Stack & Env Spoofing ]
local old
old = hookfunction(getrenv().debug.info, function(f, t)
    if type(f) == "function" then return "[C]" end
    if f == 4 and t == "s" then return "ReplicatedStorage.Controllers.SwordsController " end
    return old(f, t)
end)

local old2
old2 = hookfunction(getrenv().getfenv, function(l)
    if l ~= nil and type(l) == "number" then
        if l >= 1 and l <= 10 then return old2(10) end
    end
    return old2(l)
end)

-- [ Anti-Cheat Bypass Layer 2: Namecall Intercept ]
local getnamecallmethod = getnamecallmethod or get_namecall_method
local hookmetamethod = hookmetamethod or hook_meta_method
local oldNamecall

oldNamecall = hookmetamethod(game, "__namecall", function(self, ...)
    local method = getnamecallmethod()
    
    if not checkcaller() then
        if method == "Kick" or method == "kick" then
            return -- Block client kicks
        end
        if method == "FireServer" and self.Name == "Security" then
            return -- Block security remote triggers
        end
    end
    
    return oldNamecall(self, ...)
end)

-- ==========================================
-- ASCEND UI LOADER
-- ==========================================
local success, AscendUI = pcall(function()
    return loadstring(game:HttpGet("https://raw.githubusercontent.com/kekijak142/fg/refs/heads/main/gfh"))()
end)

if not success or type(AscendUI) ~= "table" then
    warn("Failed to load Ascend UI from GitHub. Please check the URL or your executor's loadstring functionality.")
    return
end

local Window = AscendUI:CreateWindow("Ascend Hub")

-- ==========================================
-- SCRIPT LOGIC & FEATURES
-- ==========================================

local getgenv = getgenv or function() return _G end
getgenv().AutoParry = false
getgenv().AutoSpam = false
getgenv().ParryDistance = 25

local function getBall()
    for _, v in pairs(workspace:GetChildren()) do
        if v.Name == "Ball" and v:IsA("BasePart") then
            return v
        end
    end
    return nil
end

local function triggerParry()
    -- Emulate a realistic click to bypass VIM detection
    VirtualInputManager:SendMouseButtonEvent(0, 0, 0, true, game, 1)
    task.wait(0.01)
    VirtualInputManager:SendMouseButtonEvent(0, 0, 0, false, game, 1)
    
    -- Fallback remote fire if VIM fails
    pcall(function()
        ReplicatedStorage.Remotes.ParryAttempt:FireServer(0.5, workspace.CurrentCamera.CFrame, {}, {0, 0})
    end)
end

RunService.RenderStepped:Connect(function()
    if getgenv().AutoParry and LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
        local ball = getBall()
        if ball then
            local root = LocalPlayer.Character.HumanoidRootPart
            local distance = (ball.Position - root.Position).Magnitude
            local ballVelocity = ball.Velocity.Magnitude
            
            -- Time to impact calculation
            local timeToImpact = distance / (ballVelocity == 0 and 1 or ballVelocity)
            
            -- Check if ball is targeting us (indicated by red highlight in most versions)
            local isTargeted = ball:FindFirstChild("Highlight") and ball.Highlight.FillColor == Color3.fromRGB(255, 0, 0)
            
            if isTargeted and timeToImpact <= 0.45 then
                triggerParry()
            end
            
            if getgenv().AutoSpam and distance < getgenv().ParryDistance then
                triggerParry()
            end
        end
    end
end)

-- ==========================================
-- TABS & TOGGLES
-- ==========================================

local CombatTab = Window:CreateTab("Combat")

CombatTab:CreateSection("Auto Parry Settings")
CombatTab:CreateToggle("Enable Auto Parry", false, function(state)
    getgenv().AutoParry = state
end)

CombatTab:CreateToggle("Auto Spam (Close Range)", false, function(state)
    getgenv().AutoSpam = state
end)

CombatTab:CreateSlider("Parry Distance", 10, 100, 25, function(val)
    getgenv().ParryDistance = val
end)

local MiscTab = Window:CreateTab("Misc")
MiscTab:CreateSection("Security")
MiscTab:CreateButton("Anti-Cheat Bypass Active", function()
    -- Visual indicator only
    print("Bypass is fully active!")
end)

print("Ascend Hub (Web Loader) Loaded Successfully.")
